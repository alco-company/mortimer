<div
  data-controller="resource--input-calendar-component"
  data-resource--input-calendar-component-marked-days-value="[<%= @resource.rrule %>]"
  class="space-y-1 px-4 sm:space-y-0  sm:px-6 sm:py-5">
  <%= @form.hidden_field @field, data: { "resource--input-calendar-component-target": "input"} %>
  <div class="mt-10 text-center lg:col-start-8 lg:col-end-13 lg:row-start-1 lg:mt-9 xl:col-start-9">
    <% unless @week_planning %>
      <div class="flex items-center text-gray-900">
        <button type="button" class="-m-1.5 flex flex-none items-center justify-center p-1.5 text-gray-400 hover:text-gray-500">
          <span class="sr-only">Previous month</span>
          <!-- Heroicon name: mini/chevron-left -->
          <svg class="h-5 w-5" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
            <path fill-rule="evenodd" d="M12.79 5.23a.75.75 0 01-.02 1.06L8.832 10l3.938 3.71a.75.75 0 11-1.04 1.08l-4.5-4.25a.75.75 0 010-1.08l4.5-4.25a.75.75 0 011.06.02z" clip-rule="evenodd" />
          </svg>
        </button>
        <div class="flex-auto font-semibold">January</div>
        <button type="button" class="-m-1.5 flex flex-none items-center justify-center p-1.5 text-gray-400 hover:text-gray-500">
          <span class="sr-only">Next month</span>
          <!-- Heroicon name: mini/chevron-right -->
          <svg class="h-5 w-5" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
            <path fill-rule="evenodd" d="M7.21 14.77a.75.75 0 01.02-1.06L11.168 10 7.23 6.29a.75.75 0 111.04-1.08l4.5 4.25a.75.75 0 010 1.08l-4.5 4.25a.75.75 0 01-1.06-.02z" clip-rule="evenodd" />
          </svg>
        </button>
      </div>
    <% end %>
    <div class="mt-6 grid grid-cols-8 text-xs leading-7 text-gray-500">
      <div class="cursor-pointer"
        data-action="click->resource--input-calendar-component#toggleDay" 
        data-range="all"
        data-range-index="all" 
        data-resource--input-calendar-component-target="weekday" 
        >U</div>
      <% ndx=35;helpers.weekdays(true).each do |weekday| %>
        <div class="cursor-pointer"
          data-action="click->resource--input-calendar-component#toggleDay" 
          data-range="weekday"
          data-range-index="<%= ndx=ndx+1 %>" 
          data-resource--input-calendar-component-target="weekday" >
          <%= weekday %>
        </div>
      <% end %>
    </div>
    <div class="isolate mt-2 grid grid-cols-8 gap-px rounded-lg bg-gray-200 text-sm shadow ring-1 ring-gray-200">
      <% ndx=1; week=0; day=@start_date; while day < @end_date do %>
        <% if (ndx % (week*7+1) == 0) %>
          <!-- Week -->
          <button 
            data-action="resource--input-calendar-component#toggleDay" 
            data-range="week" 
            data-range-index="<%= ndx %>"
            data-resource--input-calendar-component-target="week" 
            type="button" 
            class="<%= helpers.day_classes week %> bg-gray-100 border-r border-r-gray-300 py-1.5 text-gray-400 hover:bg-gray-100 focus:z-10">
            <time datetime="<%= l day, format: :short_iso %>" class="<%= helpers.time_classes day %> mx-auto flex h-7 w-7 items-center justify-center rounded-full">
              <%= helpers.week_number day, (week=week+1), @week_planning %>
            </time>
          </button>
        <% end %>

        <button 
          data-action="resource--input-calendar-component#toggleDay" 
          data-range="day" 
          data-range-index="<%= ndx %>"
          data-resource--input-calendar-component-target="day" 
          type="button" 
          class=" bg-gray-50 py-1.5 text-gray-400 hover:bg-gray-100 focus:z-10">
          <time  datetime="<%= l day, format: :short_iso %>" class="mx-auto flex h-7 w-7 items-center justify-center rounded-full">
            <%= helpers.day_number day, @week_planning %>
          </time>
        </button>
        <% ndx=ndx+1; day=day + 1.day %>

      <% end  %>
    </div>
    <div class="mt-6 grid grid-cols-8 text-xs leading-3 text-gray-500">
      <div class="cursor-pointer" data-action="click->resource--input-calendar-component#clearAll">
        <%= t "clear_all_calendar" %>
      </div>
    </div>

    <%# <button 
      type="button" 
      class="mt-8 w-full rounded-md border border-transparent bg-indigo-600 py-2 px-4 text-sm font-medium text-white shadow hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
      Add event
    </button> %>
  </div>
</div>

