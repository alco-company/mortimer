<%= form_with(model: pupil_transaction) do |form| %>
  <% if pupil_transaction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pupil_transaction.errors.count, "error") %> prohibited this pupil_transaction from being saved:</h2>

      <ul>
        <% pupil_transaction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :asset_id, style: "display: block" %>
    <%= form.text_field :asset_id %>
  </div>

  <div>
    <%= form.label :pupil_id, style: "display: block" %>
    <%= form.text_field :pupil_id %>
  </div>

  <div>
    <%= form.label :work_minutes, style: "display: block" %>
    <%= form.number_field :work_minutes %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
