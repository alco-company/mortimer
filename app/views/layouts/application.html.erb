<!DOCTYPE html>
<html class="h-full bg-white">
  <head>
    <title>S P E I C H E R</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://fonts.sandbox.google.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= yield :head %>
    <% if Rails.env.development? %>
      <%= javascript_include_tag "hotwire-livereload", defer: true %>
    <% end %>
  </head>

  <body class="h-full">
    <div class="min-h-full">

      <!-- Static sidebar for desktop -->
      <%= render Navigation::SidebarComponent.new current: nil %>

      <!-- Main column -->
      <div class="lg:pl-64 flex flex-col">
        <main class="flex-1">
          <%= yield %>
        </main>
      </div>
    </div>

    <!-- Primary Data Entry Form - slide-over from right -->
    <%= turbo_frame_tag 'form_slideover' do %>
      <%= render Resource::FormComponent.new %>
    <% end %>

    <div id="modal_content"></div>

    <%= render "shared/takiro" %>
    <%= render partial: "shared/notifications" %>
    <div id="unauthorized"></div>
  </body>
</html>
