<!DOCTYPE html>
<html class="h-full">
  <head>
    <title>Mortimer</title>
    <meta name="viewport" content="width=device-width,initial-scale=1,minimum-scale=1,maximum-scale=1,user-scalable=no">
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-title" content="Mortimer" />
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black" />

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%# now should be handled by the next 'tag' (javascript_importmap_tags) = javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%# importmap disabled to enable hotwire-livereload %>
    <%#= javascript_importmap_tags %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= yield :head %>
    <% if Rails.env.development? %>
      <%= javascript_include_tag "hotwire-livereload", defer: true %>
    <% end %>
  </head>

  <body 
    id="switchboard" 
    class="h-full bg-slate-100" 
    data-controller="switchboard" 
    data-action="speicherMessage@window->switchboard#handleMessages"
    >

    <div data-controller="extended-slideover" 
         data-extended-slideover-active-target="#extended-slideover-target"
         data-action="speicherMessage@window->extended-slideover#handleMessages"
      >

      <!-- Canvas-->
      <div class="flex flex-col flex-1">

        <!-- topbar -->
        <%#= render Topbar::Component.new( current_user: Current.user, placeholder: raw( t('topbar.search'))) %>

        <main>
          <div class="py-6">
            <%= yield %>
          </div>
        </main>

      </div>
      <%= turbo_frame_tag 'form_slideover'  %>
      <%= render Resource::FormComponent.new %>
      <div id="modal_content"></div>

    </div>

    <%#= render "shared/takiro" %>
    <%= render partial: "shared/notifications" %>

    <%= render partial: "pos/employees/footer" %>
  </body>    


</html>


